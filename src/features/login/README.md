# Login feature

## Purpose

Renders the Supabase email/password sign-in entry point.

## Components

Uses the feature-owned `components/login-form.tsx` component.

## Dependencies

Next.js navigation and the authentication API route.

## Exports

Default `LoginPage` component.
